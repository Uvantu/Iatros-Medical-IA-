# Data

La base `entrenador-virtual.sqlite` no se sube en esta copia porque es un archivo binario local.

El programa la recrea automaticamente al arrancar `server.js` y, si no existe, siembra la base inicial desde `src/seed.js`.
